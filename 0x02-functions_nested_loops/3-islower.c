#include"main.h"
/**
 *  islawoer - check if character is lowercase or not
 *
 *  Return 1 if c is lowercase and 0 if not
 */
int _islower(int c)
{
	if (c >= 97 && c c <= 122)
		return (1);
	return (0);
}
